#include <iostream>
using namespace std;
int main()
{
    int arr[5] = [ 10, 20, 30, 40, 50 ] int n = arr.size();
    int k = 3;
    vectore<int>nums(m);

    int tem = arr[n-1]
        //   while (k)
        //   {
        //   int tem = (n-1);
        //   for (int i = n-2; i >=0; i++)
        //   {
        //      arr[i]= arr[i+1];
        //   }
        //   arr[0]=tem;
        //  }

        for (int i = 0; i <n; i++)
    {
        arr[(i + k) % n] = arr[i]
    }
     arr[i] = tem 
     for (int i = 0; i < n; i++)
     {
         arr[i] = nums[i]
     }
     
}